# URL file for handling '' path (eProc_Registration app)
from django.urls import path
from . import views

app_name = 'eProc_Registration'
# Defining the mapping between URLs and views
urlpatterns = [

]
